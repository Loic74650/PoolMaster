if real probe do not store MQTT air temp


Settings:
- allocate found addresses to which temp
- reset search for addresses
- 


Flah red warning when errors
Button to clear errors
display firmware revisions (screen and µC)
JSON command to allocate DS18B20 probes

Tank change

Probes calib not great. Single point calib resets the slope so must be performed twice

Nextion Alpha Numerical keyboard:

stay pressed on a key, display changes from 1 to A to B to C...
How to implement: 
key press starts timer with 300ms delay
key release stops timer
every timer event, it toggles the display

Delay PID not working
clear errors not restarting pumps

as different maxtimes 